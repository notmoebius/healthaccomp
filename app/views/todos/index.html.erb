<p id="notice"><%= notice %></p>

<h1>Todos</h1>

<table>
  <thead>
    <tr>
      <th>Label</th>
      <th>Action desc</th>
      <th>Signalement</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <td><%= todo.label %></td>
        <td><%= todo.action_desc %></td>
        <td><%= todo.report_id %></td>
        <td><%= link_to 'Voir', todo %></td>
        <td><%= link_to 'Editer', edit_todo_path(todo) %></td>
        <td><%= link_to 'Supprimer', todo, method: :delete, data: { confirm: 'Etes vous sûr ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ajouter une action', new_todo_path %>
